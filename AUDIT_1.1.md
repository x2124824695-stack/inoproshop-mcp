# 汇川 MCP 框架审核与修复记录

日期：2026-09-21。版本：1.0.0 → 1.1.0。

审核范围为当前 `src` 中的 MCP 服务、工具策略、文件 IPC、Watcher、脚本模板及测试链路。保留常驻 InoProShop 架构，未修改、保存、下载或连接生产 PLC 工程。修改前源码、测试和主要配置已备份在工作区 `review/mcp-audit-2026-09-21/before`。

## 已修复

| 问题 | 原来可能出现的行为 | 1.1 的处理 |
|---|---|---|
| 请求异常后重复执行 | Watcher 在格式化异常或写结果失败后，再次轮询同一 `.command.json` | 执行前原子改名为 `.running.json`；轮询不执行已领取请求。结果丢失时保留未知状态，不自动重发 |
| 中文结果与异常 | 默认 JSON ASCII 编码异常；中文异常再次触发编码错误；UTF-8 字节被捕获为 Unicode 后产生乱码 | 模板使用 Unicode 字面量和 LF；Watcher 使用 `ensure_ascii=False`；捕获器直接接收 Unicode；异常转换有后备路径 |
| 返回错误却报告成功 | `SystemExit(1)` 可被成功标记覆盖；解析失败分支可返回空的成功消息 | 非零退出、缺失成功标记、错误标记和解析失败都返回失败 |
| 只读会话无法恢复 | `recover_session` 未注册，已完成的超时请求仍锁死 | 只读模式允许核对已完成结果；未知结果仍不能解除锁 |
| Node 意外退出后丢失未决请求 | Node 尚未写超时锁，重连可能漏掉已提交请求 | 重新发现 `.command.json` / `.running.json` 并生成未决记录；确认结果后先清理派发文件再解锁 |
| 工具返回不一致 | 搜索仅有文本，客户端无法读取 `hits`；不同工具结构不同 | 所有工具提供统一 `structuredContent` 外层，保留旧文本；搜索、引用和全量代码提供真实结构化结果 |
| 检索覆盖不透明 | 节点访问异常被吞掉，零命中容易被当作没有引用 | 返回 `read_errors`、`complete`、图形语言跳过信息；输出提示检索不完整。命中上限只有确实漏掉额外命中时才标记截断 |
| 全量读取静默漏代码 | 子树或文本读取异常仍报告全量读取成功 | 遍历失败明确返回错误 |
| 只读退出可能保存工程 | `--read-only --no-keep-alive` 仍可能调用保存 | 只读退出仅释放 MCP 所有权 |
| 升级可能另开 IDE | 改 Watcher 版本同时改变会话目录，绕开旧活跃会话 | 保留会话命名空间，单独校验运行协议版本；旧版仍活跃时明确拒绝启动第二个实例 |
| 资源文本损坏 | 读取资源将源码中的字面量 `\n` 替换为换行 | 保留源码字面量，缺失结果标记时返回错误 |
| 测试可能测到旧产物 | stdio 测试在构建之前启动旧 `dist` | `npm run check` 先构建再测试，发布检查也使用完整检查链 |

另补充了模板文件名校验和控制字符转义。原有路径限制、工程备份、代码哈希、修改回滚、默认关闭在线工具等行为保留。

## 验证结果

- TypeScript 类型检查和发布构建通过。
- Node/stdio：25 项通过，包含工具发现、只读恢复、结构化结果、重连未决请求与未领取结果、结果清理时序。
- Python：26 项通过，包含原有代码回滚/在线语义测试及新增 Watcher 故障注入、中文检索、截断与覆盖提示测试。
- 额外使用本机汇川安装目录中的 **IronPython 2.7.7** 和实际 .NET 文件/事件 API，通过中文结果写入读取、中文异常、非零退出、同一请求不重复执行、中文搜索和全量读取验证。
- IronPython 验证使用模拟 `scriptengine`。没有验证 IDE 主线程真实调度、设备通信或在线写入；这些不能由上述通过结果推断。

复现通用检查：

```powershell
npm run check
```

复现本机 IronPython 检查（在项目根目录，DLL 和标准库路径按安装位置调整）：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tests/run-ironpython.ps1 `
  -EngineDir 'D:\HuiChuang\CODESYS\PlugIns\dc937c18-e9f0-434d-85b7-1b8f499e378a\3.5.11.10' `
  -StdLib 'D:\HuiChuang\CODESYS\ScriptLib\3.5.10.40'
```

## 升级与剩余边界

新版源码及 `dist` 已构建；新安装包为 `inoproshop-mcp-improved-1.1.0.tgz`。已运行的 Node/MCP 进程和 IDE Watcher 不会自动热更新。重启 MCP 后，旧 Watcher 若仍活跃，会提示处理未决结果、按现场流程保存关闭旧 IDE，再显式启动新版。此过程不会自动保存或结束生产 IDE。

无法得到最终结果的请求保持 `unknown`，需人工核对，不能强制确认成功。本版保证正常 IPC 调度不重放已领取请求，不等同于断电或磁盘损坏情况下的分布式“恰好执行一次”。

搜索面向文本语言；图形语言没有完整语义覆盖。任意正则仍在 IDE 线程执行，极端正则可能耗时；命中数量、节点数量和递归深度限制不是正则运行时间限制。大型工程全量读取仍可能生成较大响应，日常优先用精确 POU 读取和限定检索。

设备描述符、私有反射兼容层、在线写入、任务配置及真实 UI 调度仍需副本工程或隔离台架验证。本版未做依赖漏洞数据库审计，也没有将库版本升级与可靠性修复混在一起。
