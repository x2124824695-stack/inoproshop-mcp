# InoProShop MCP 改进计划

基线：用户提供的 2026-09-15 RAR（codesys-mcp-persistent 0.6.3），结合 LIMIT-LMT/InoProShop_LIMIT_MCP 的 24 工具版本。原始文件保留在相邻 review 目录，不覆盖。

1. 保留常驻 UI、文件 IPC、独立 Python 模板；补全 GitHub 版的项目树、单对象读取、移动、任务配置和设备参数读取能力。
2. 修复写入误报、编译 fatal 漏报、参数二次插值、中文编码和字符串结果标记碰撞。写代码支持旧值哈希校验、读回校验和失败回滚。
3. 重做实例生命周期：明确 profile/exe/workspace 身份、版本握手、单服务所有权锁、真实 readiness；默认保留 IDE，不强杀用户进程，不自动切换执行模式。
4. 超时不等于取消。超时请求进入待核对状态，阻止后续修改，提供结果查询和显式确认恢复；全工具串行化，落盘备份和不含代码/凭据的审计日志。
5. 在线工具默认不暴露；显式开启后，连接只连接，普通写值不强制，下载模式采用官方枚举语义。危险操作使用明确的工具与参数，不依赖模型自觉。
6. 补充单元测试、假 ScriptEngine 行为测试、stdio 协议测试；交付中文 README、配置示例、原始问题证据及实机验收表。

验收边界：本机不假设存在 InoProShop 或 PLC。自动测试证明可隔离验证的行为；SP11、设备库、任务周期单位、在线操作必须由指定版本仿真/台架验收，不能将注册成功视为实机支持。
