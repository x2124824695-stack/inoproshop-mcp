# 验收清单

## 1.1 自动验证记录（2026-09-21）

`npm run check`：构建、25 项 Node/stdio、26 项 Python 测试通过。`tests/run-ironpython.ps1`：使用本机 IronPython 2.7.7 和模拟 ScriptEngine，通过中文检索/全量读取、Watcher 结果与异常、非零退出及防重放验证。详见 [审核记录](AUDIT_1.1.md)。以下真实 IDE 和 PLC 项目尚不能由这些测试代替。

新增现场验收项：在副本工程检查新 Watcher 主线程调度；中文路径、中文检索和编译消息应无乱码；用旧版活跃 Watcher 启动新服务时应拒绝另开 IDE；只读模式即使指定 `--no-keep-alive` 也不得保存工程。

自动化基线已覆盖 TypeScript 编译、stdio 握手与工具发现、路径逃逸、策略开关、串行队列、备份/审计脱敏、会话身份、超时结果对账、中文/占位符插值、代码写入回滚、旧值哈希、一次性变量写入和下载枚举语义。

以下项目需要指定 InoProShop 与 PLC 环境：

## A. 仿真工程

- 记录 InoProShop 完整版本、内核 SP、Profile 字符串、Node 版本。
- 启动服务，验证 `get_codesys_status`、自动接管和第二服务所有权拒绝。
- 打开含中文路径、中文注释、中文字符串的副本工程。
- 读取 POU，修改声明或实现，验证 hash、读回、保存、重新打开和编译。
- 制造语法错误，确认 `compile_project` 返回错误、对象名和行号；清除后确认零错误。
- 对保存失败、编辑器 replace 失败和命令超时做故障注入，确认没有误报成功或自动重试。
- 创建循环任务，分别使用 us/ms/s，重开工程验证周期、单位、优先级和 POU 列表。
- 对对象移动、设备参数、库、归档和 I/O 映射逐工具验证；不支持的描述符必须明确失败。

## B. PLC 隔离台架

- 使用无机械负载或已隔离输出的测试 PLC；记录固件、网关和设备包版本。
- `connect_to_device` 不应触发下载；断开后程序保持不变。
- `write_variable` 写入一次后允许 PLC 程序在下一周期覆盖；`get_forced_expressions()` 应为空。
- 预先在 IDE 准备另一个变量值，确认 MCP 拒绝写入而不提交它。
- `online_change` 不可行时必须失败且只调用一次，不得完整下载。
- `full` 缺少 `confirmFullDownload=true` 时必须在 MCP 端拒绝；提供确认后验证完整下载语义。
- 下载后断电测试启动应用策略；当前工具不会自动创建 boot application，应按现场流程显式处理。
- 验证启停、凭据、仿真切换、批量读取和最长 1 秒监控。

## C. 通过标准

- 所有修改前有可读备份，失败后工程可重新打开。
- 工具返回成功与 IDE/PLC 的可观察结果一致。
- 超时后同一会话被冻结，结果核对前不能执行下一条命令。
- 没有强杀非本服务启动的 IDE，没有自动完整下载，没有隐式强制变量。
- 中文工程在读取、写入、编译诊断和重开后保持一致。
